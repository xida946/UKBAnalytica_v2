% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable_preprocess.R
\name{preprocess_baseline}
\alias{preprocess_baseline}
\title{Preprocess UKB baseline variables}
\usage{
preprocess_baseline(
  df,
  variables,
  custom_mapping = NULL,
  missing_action = c("keep", "drop"),
  invalid_codes = c(-1, -3)
)
}
\arguments{
\item{df}{A data.table or data.frame containing UKB data from rap platform export.}

\item{variables}{Character vector of variable names to process.
Use \code{get_variable_info()} to see available variables.}

\item{custom_mapping}{Optional named list for user-defined variable mappings.
Each element should have: \code{ukb_col} (required), \code{description} (optional).
Example: \code{list(my_var = list(ukb_col = "p12345_i0", description = "My custom var"))}}

\item{missing_action}{Character. How to handle missing values:
\itemize{
\item "keep": Keep as NA (default)
\item "drop": Remove rows with any missing values in processed variables
}}

\item{invalid_codes}{Numeric vector of UKB codes to treat as missing.
Default: c(-1, -3) which are "Prefer not to answer" and "Do not know"}
}
\value{
A data.table with original data plus processed variable columns
}
\description{
A unified function to preprocess UKB baseline characteristics with automatic
field mapping and standardized transformations.
}
\examples{
\dontrun{
# Process predefined variables
result <- preprocess_baseline(ukb_data, 
                              variables = c("sex", "age", "ethnicity", "bmi"))

# Use custom variable mapping
custom <- list(
  my_biomarker = list(ukb_col = "p30000_i0", description = "Custom biomarker")
)
result <- preprocess_baseline(ukb_data, 
                              variables = c("sex", "my_biomarker"),
                              custom_mapping = custom)

# Drop rows with missing values
result <- preprocess_baseline(ukb_data, 
                              variables = c("sex", "age"),
                              missing_action = "drop")
}

}
