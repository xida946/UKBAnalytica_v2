% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{sensitivity_exclude_missing_covariates}
\alias{sensitivity_exclude_missing_covariates}
\title{Exclude Rows with Missing Covariates for Sensitivity Analysis}
\usage{
sensitivity_exclude_missing_covariates(
  data,
  covariates,
  copy = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data.frame or data.table.}

\item{covariates}{Character vector of covariate names to check.}

\item{copy}{Logical scalar. If \code{TRUE} and \code{data} is a data.table, work on a
copied object before filtering.}

\item{verbose}{Logical scalar. If \code{TRUE}, print a short filtering summary.}
}
\value{
An object with the same class and columns as \code{data}, with filtered
rows removed. A \code{sensitivity_info} attribute is added for auditability.
}
\description{
Remove participants with missing values in any of the specified covariates.
The returned dataset keeps the same columns and class as the input so it can
be passed directly to the standard regression functions.
}
\examples{
dt_sens <- sensitivity_exclude_missing_covariates(
  data = mtcars,
  covariates = c("hp", "wt")
)

}
