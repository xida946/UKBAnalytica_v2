% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{runmulti_logit}
\alias{runmulti_logit}
\title{Run multiple logistic regression models}
\usage{
runmulti_logit(data, main_var, covariates = NULL, outcome, ...)
}
\arguments{
\item{data}{A data.frame or data.table containing all variables.}

\item{main_var}{A character vector of main variable names to test.}

\item{covariates}{A character vector of covariate names to adjust for. Default \code{NULL} (univariate).}

\item{outcome}{A character string specifying the binary outcome (dependent) variable name (0/1).}

\item{...}{Additional arguments passed to \code{stats::glm()}.}
}
\value{
A data.frame with columns: \code{variable}, \code{OR}, \code{lower95}, \code{upper95}, \code{pvalue}.
}
\description{
Fit logistic regression models (\code{glm} with \code{family = binomial}) for each main variable separately.
When \code{covariates} is \code{NULL}, univariate models are fitted.
Otherwise, multivariate models adjusting for the specified covariates are fitted.
}
\examples{
\dontrun{
# Create binary outcome
mtcars$am_bin <- ifelse(mtcars$am == 1, 1, 0)

# Univariate logistic regression
res <- runmulti_logit(mtcars, main_var = c("hp", "wt"), outcome = "am_bin")

# Multivariate logistic regression
res <- runmulti_logit(mtcars, main_var = c("hp", "wt"),
                      covariates = c("cyl"), outcome = "am_bin")
}

}
