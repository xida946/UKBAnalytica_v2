% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{runmulti_cox}
\alias{runmulti_cox}
\title{Run multiple Cox proportional hazards models}
\usage{
runmulti_cox(
  data,
  main_var,
  covariates = NULL,
  endpoint = c("time", "status"),
  ...
)
}
\arguments{
\item{data}{A data.frame or data.table containing all variables.}

\item{main_var}{A character vector of main variable names to test.}

\item{covariates}{A character vector of covariate names to adjust for. Default \code{NULL} (univariate).}

\item{endpoint}{A character vector of length 2: \code{c("time", "status")}, indicating survival time and event columns.}

\item{...}{Additional arguments passed to \code{survival::coxph()}.}
}
\value{
A data.frame with columns: \code{variable}, \code{HR}, \code{lower95}, \code{upper95}, \code{pvalue}.
}
\description{
Fit Cox proportional hazards models for each main variable separately.
When \code{covariates} is \code{NULL}, univariate models are fitted.
Otherwise, multivariate models adjusting for the specified covariates are fitted.
}
\examples{
\dontrun{
library(survival)
# Univariate Cox
res <- runmulti_cox(lung, main_var = c("age", "sex"), endpoint = c("time", "status"))

# Multivariate Cox
res <- runmulti_cox(lung, main_var = c("age", "sex"),
                    covariates = c("ph.ecog"), endpoint = c("time", "status"))
}

}
