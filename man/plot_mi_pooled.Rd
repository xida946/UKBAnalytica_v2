% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_mi_pooled}
\alias{plot_mi_pooled}
\title{Plot Multiple Imputation Pooled Results}
\usage{
plot_mi_pooled(
  mi_result,
  terms = NULL,
  exponentiate = NULL,
  null_value = NULL,
  title = "Pooled Estimates (Multiple Imputation)",
  colors = NULL,
  show_fmi = TRUE
)
}
\arguments{
\item{mi_result}{An object of class \code{mi_pooled_result} from \code{pool_mi_models()}.}

\item{terms}{Character vector of terms to include. If NULL, all terms except
intercept are shown.}

\item{exponentiate}{Logical; whether to exponentiate estimates. If NULL,
uses the setting from the mi_result object.}

\item{null_value}{Numeric; reference line value. If NULL, automatically set
based on exponentiation (0 for linear scale, 1 for exp scale).}

\item{title}{Character string for plot title.}

\item{colors}{Named character vector for colors. Default uses package palette.}

\item{show_fmi}{Logical; whether to display FMI (Fraction of Missing Information)
as point size or annotation. Default TRUE.}
}
\value{
A ggplot2 object.
}
\description{
Creates a forest plot for pooled estimates from multiple imputation analysis.
}
\examples{
\dontrun{
pooled <- pool_mi_models(models, model_type = "cox")
plot_mi_pooled(pooled)
plot_mi_pooled(pooled, exponentiate = TRUE, show_fmi = TRUE)
}

}
