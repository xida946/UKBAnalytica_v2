% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{plot_correlation}
\alias{plot_correlation}
\title{Visualize correlation matrix as a heatmap}
\usage{
plot_correlation(
  corr_matrix,
  title = "Correlation Matrix",
  show_values = TRUE,
  digits = 2,
  text_size = 3,
  color_low = "#3B4CC0",
  color_mid = "white",
  color_high = "#B40426",
  upper_triangle = FALSE
)
}
\arguments{
\item{corr_matrix}{A numeric correlation matrix (from \code{run_correlation()} or \code{cor()}).}

\item{title}{Character string. Plot title. Default: \code{"Correlation Matrix"}.}

\item{show_values}{Logical. If \code{TRUE}, display correlation values on tiles. Default: \code{TRUE}.}

\item{digits}{Integer. Number of decimal places for correlation values. Default: 2.}

\item{text_size}{Numeric. Size of text labels on tiles. Default: 3.}

\item{color_low}{Character. Color for negative correlations. Default: \code{"#3B4CC0"} (blue).}

\item{color_mid}{Character. Color for zero correlation. Default: \code{"white"}.}

\item{color_high}{Character. Color for positive correlations. Default: \code{"#B40426"} (red).}

\item{upper_triangle}{Logical. If \code{TRUE}, show only upper triangle. Default: \code{FALSE}.}
}
\value{
A \code{ggplot2} object. Can be further customized with \code{ggplot2} functions.
}
\description{
Create an annotated heatmap of a correlation matrix with customizable appearance.
This helps identify patterns, multicollinearity, and variable relationships visually.
}
\examples{
\dontrun{
library(data.table)
dt <- data.table(x = rnorm(100), y = rnorm(100), z = rnorm(100))
corr_mat <- run_correlation(dt, vars = c("x", "y", "z"))

# Basic heatmap
plot_correlation(corr_mat)

# Upper triangle only without values
plot_correlation(
  corr_mat,
  show_values = FALSE,
  upper_triangle = TRUE
)
}

}
