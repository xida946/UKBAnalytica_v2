% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_model.R
\name{ukb_ml_model}
\alias{ukb_ml_model}
\title{Train a Machine Learning Model}
\usage{
ukb_ml_model(
  formula,
  data,
  model = c("rf", "xgboost", "glmnet", "svm", "nnet", "logistic"),
  task = c("classification", "regression"),
  split_ratio = 0.8,
  stratify = TRUE,
  seed = NULL,
  sample_n = NULL,
  params = list(),
  cv = FALSE,
  cv_folds = 5,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{formula}{Model formula (e.g., outcome ~ var1 + var2)}

\item{data}{Data frame containing variables}

\item{model}{Model type: "rf", "xgboost", "glmnet", "svm", "nnet", "logistic"}

\item{task}{Task type: "classification" or "regression"}

\item{split_ratio}{Train/test split ratio (default 0.8)}

\item{stratify}{Logical; use stratified sampling for classification (default TRUE)}

\item{seed}{Random seed for reproducibility}

\item{sample_n}{Optional; subsample data for large datasets}

\item{params}{List of model-specific parameters}

\item{cv}{Logical; perform cross-validation (default FALSE)}

\item{cv_folds}{Number of CV folds (default 5)}

\item{verbose}{Logical; print progress messages}

\item{...}{Additional arguments passed to model function}
}
\value{
An object of class "ukb_ml" containing:
\itemize{
\item model: The fitted model object
\item model_type: Type of model used
\item task: Task type (classification/regression)
\item predictors: Names of predictor variables
\item outcome: Name of outcome variable
\item train_data: Training data
\item test_data: Test data
\item metrics: Model performance metrics
}
}
\description{
Unified interface for training machine learning models on UK Biobank data.
Supports random forest, XGBoost, elastic net, SVM, and neural networks.
}
\examples{
\dontrun{
# Random Forest classification
ml_rf <- ukb_ml_model(
  diabetes ~ age + bmi + sbp + smoking,
  data = ukb_data,
  model = "rf",
  task = "classification"
)

# XGBoost with custom parameters
ml_xgb <- ukb_ml_model(
  bmi ~ age + sex + income,
  data = ukb_data,
  model = "xgboost",
  task = "regression",
  params = list(nrounds = 100, max_depth = 6)
)
}

}
