% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propensity.R
\name{match_propensity}
\alias{match_propensity}
\title{Propensity Score Matching}
\usage{
match_propensity(
  data,
  ps_col = "ps",
  treatment,
  ratio = 1,
  caliper = 0.2,
  method = c("nearest", "optimal"),
  replace = FALSE,
  exact_match = NULL
)
}
\arguments{
\item{data}{A data.table containing propensity scores.}

\item{ps_col}{Character string specifying the propensity score column name. Default "ps".}

\item{treatment}{Character string specifying the treatment variable name.}

\item{ratio}{Numeric matching ratio (1:ratio). Default 1 for 1:1 matching.}

\item{caliper}{Numeric caliper width in standard deviations of PS. Default 0.2.}

\item{method}{Character string specifying matching method: "nearest" or "optimal".}

\item{replace}{Logical; whether to match with replacement. Default FALSE.}

\item{exact_match}{Character vector of variable names for exact matching. Default NULL.}
}
\value{
A data.table with matched data, including:
\describe{
\item{match_id}{Matched pair identifier}
\item{match_distance}{Distance between matched pairs}
}
}
\description{
Perform propensity score matching using nearest neighbor or optimal matching.
}
\examples{
\dontrun{
# First estimate PS
ps_data <- estimate_propensity_score(mydata, "treated", c("age", "sex"))

# Then match
matched_data <- match_propensity(
  ps_data,
  treatment = "treated",
  ratio = 1,
  caliper = 0.2
)
}

}
