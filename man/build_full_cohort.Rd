% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival.R
\name{build_full_cohort}
\alias{build_full_cohort}
\title{Build Full Cohort Survival Dataset}
\usage{
build_full_cohort(
  dt,
  disease_definitions,
  prevalent_sources = c("ICD10", "ICD9", "Self-report", "Death"),
  outcome_sources = c("ICD10", "ICD9", "Death"),
  censor_date = as.Date("2023-10-31"),
  baseline_col = "p53_i0",
  primary_disease = NULL,
  exclude_prevalent = TRUE
)
}
\arguments{
\item{dt}{A data.table or data.frame containing complete UKB data.}

\item{disease_definitions}{Named list of disease definitions (see \code{\link{create_disease_definition}}).}

\item{prevalent_sources}{Character vector specifying data sources for identifying
prevalent (baseline) cases. Self-report is recommended here since participants
reporting a disease at baseline clearly had it before enrollment. Default includes
all sources: "ICD10", "ICD9", "Self-report", "Death".}

\item{outcome_sources}{Character vector specifying data sources for defining
incident outcomes. Self-report is typically excluded here because self-reported
diagnosis dates are imprecise (year only) and less reliable for prospective
endpoint ascertainment. Default: "ICD10", "ICD9", "Death".}

\item{censor_date}{Administrative censoring date (default: "2023-10-31").}

\item{baseline_col}{Column name for baseline assessment date (default: "p53_i0").}

\item{primary_disease}{Disease key used to compute follow-up time and event
status (must be in \code{disease_definitions}). If NULL, the first disease
in the list is used.}

\item{exclude_prevalent}{Logical; if TRUE, excludes prevalent cases from output.}
}
\value{
A data.table with complete cohort survival data.
}
\description{
Extends \code{\link{build_survival_dataset}} to include non-cases (controls)
for each disease, creating a complete cohort for survival analysis.
}
\keyword{internal}
