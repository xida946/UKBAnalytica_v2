% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subgroup.R
\name{run_subgroup_analysis}
\alias{run_subgroup_analysis}
\title{Run Subgroup Analysis}
\usage{
run_subgroup_analysis(
  data,
  exposure,
  outcome = NULL,
  subgroup_var,
  covariates = NULL,
  model_type = c("cox", "logistic", "linear"),
  endpoint = NULL,
  ref_level = NULL
)
}
\arguments{
\item{data}{A data.frame or data.table containing all variables.}

\item{exposure}{Character string specifying the exposure variable name.}

\item{outcome}{Character string specifying the outcome variable name.
For Cox models, this can be NULL if endpoint is specified.}

\item{subgroup_var}{Character string specifying the subgroup variable name.}

\item{covariates}{Character vector of covariate names to adjust for. Default NULL.}

\item{model_type}{Character string specifying model type: "cox", "logistic", or "linear".}

\item{endpoint}{Character vector of length 2 for Cox models: c("time", "status").
Required when model_type = "cox".}

\item{ref_level}{Character string specifying the reference level for the subgroup variable.
If NULL, the first level is used as reference.}
}
\value{
A data.frame with columns:
\describe{
\item{subgroup_var}{Name of the subgroup variable}
\item{subgroup}{Subgroup level}
\item{n}{Sample size in subgroup}
\item{n_event}{Number of events (for Cox/logistic models)}
\item{estimate}{Effect estimate (HR for Cox, OR for logistic, Beta for linear)}
\item{lower95}{Lower 95\\% CI}
\item{upper95}{Upper 95\\% CI}
\item{pvalue}{P-value for the exposure effect}
\item{p_interaction}{P-value for interaction between exposure and subgroup}
}
}
\description{
Perform subgroup analysis by fitting regression models within each level of a
subgroup variable and calculating interaction p-values.
}
\examples{
\dontrun{
library(survival)
# Cox model subgroup analysis
result <- run_subgroup_analysis(
  data = lung,
  exposure = "ph.ecog",
  subgroup_var = "sex",
  model_type = "cox",
  endpoint = c("time", "status")
)

# Logistic regression subgroup analysis
mtcars$am_binary <- ifelse(mtcars$am == 1, 1, 0)
result <- run_subgroup_analysis(
  data = mtcars,
  exposure = "hp",
  outcome = "am_binary",
  subgroup_var = "cyl",
  model_type = "logistic"
)
}

}
