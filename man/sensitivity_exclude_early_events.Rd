% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{sensitivity_exclude_early_events}
\alias{sensitivity_exclude_early_events}
\title{Exclude Early Events for Sensitivity Analysis}
\usage{
sensitivity_exclude_early_events(
  data,
  endpoint = c("outcome_surv_time", "outcome_status"),
  n_years,
  copy = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data.frame or data.table.}

\item{endpoint}{Character vector of length 2 giving the time and status
columns, e.g. \code{c("outcome_surv_time", "outcome_status")}.}

\item{n_years}{Numeric scalar. Events with follow-up time less than or equal
to this value will be excluded.}

\item{copy}{Logical scalar. If \code{TRUE} and \code{data} is a data.table, work on a
copied object before filtering.}

\item{verbose}{Logical scalar. If \code{TRUE}, print a short filtering summary.}
}
\value{
An object with the same class and columns as \code{data}, with filtered
rows removed. A \code{sensitivity_info} attribute is added for auditability.
}
\description{
Remove participants who experienced the event within the first \code{n_years} of
follow-up. The returned dataset keeps the same columns and class as the input
so it can be passed directly to the standard regression functions.
}
\examples{
dt_sens <- sensitivity_exclude_early_events(
  data = mtcars,
  endpoint = c("wt", "vs"),
  n_years = 3
)

}
