% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{select_incident_by_years}
\alias{select_incident_by_years}
\title{Select Incident Cases by Time Since Enrollment}
\usage{
select_incident_by_years(
  df,
  n_years = 5,
  time_col = "outcome_surv_time",
  status_col = "outcome_status",
  baseline_col = "p53_i0",
  event_date_col = "earliest_date",
  group_col = "incident_timing",
  output = c("combined", "split"),
  copy = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{df}{A data.frame or data.table.}

\item{n_years}{Numeric scalar. Cutoff in years for classifying incident
events. Default is \code{5}.}

\item{time_col}{Column name for follow-up time in years. Default is
\code{"outcome_surv_time"}.}

\item{status_col}{Column name for event status where \code{1} indicates an
incident event. Default is \code{"outcome_status"}.}

\item{baseline_col}{Column name for enrollment date. Used only when
\code{time_col} is not present. Default is \code{"p53_i0"}.}

\item{event_date_col}{Column name for event date. Used only when \code{time_col}
is not present or when \code{status_col} is unavailable. Default is
\code{"earliest_date"}.}

\item{group_col}{Name of the output grouping column. Default is
\code{"incident_timing"}.}

\item{output}{Output format. \code{"combined"} returns one filtered data object;
\code{"split"} returns a named list with two filtered data objects for events
within and after \code{n_years}.}

\item{copy}{Logical scalar. If \code{TRUE} and \code{df} is a data.table, work on a
copied object before filtering.}

\item{verbose}{Logical scalar. If \code{TRUE}, print a short selection summary.}
}
\value{
If \code{output = "combined"}, a filtered object with the same class as
\code{df}, containing only participants with incident events. The output adds
\code{group_col}. If \code{time_col} is missing but can be derived from dates, the
function also adds \code{time_col}. If \code{output = "split"}, a named list with
\code{within_n_years} and \code{after_n_years}, each preserving the same class as
\code{df}.
}
\description{
Keep only participants with incident events and classify them as occurring
within \code{n_years} or after \code{n_years} since enrollment. By default, the
function uses \code{outcome_surv_time} and \code{outcome_status} generated by
\code{\link[=build_survival_dataset]{build_survival_dataset()}}. If the follow-up time column is not available,
the function can compute it from enrollment and event dates.
}
\examples{
df <- data.frame(
  id = 1:5,
  outcome_surv_time = c(1.2, 4.9, 5.0, 8.1, 3.0),
  outcome_status = c(1, 1, 1, 1, 0)
)

result <- select_incident_by_years(df, n_years = 5)
table(result$incident_timing)

split_result <- select_incident_by_years(df, n_years = 5, output = "split")
names(split_result)

}
