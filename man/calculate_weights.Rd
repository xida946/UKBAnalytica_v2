% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propensity.R
\name{calculate_weights}
\alias{calculate_weights}
\title{Calculate IPTW Weights}
\usage{
calculate_weights(
  data,
  ps_col = "ps",
  treatment,
  weight_type = c("ATE", "ATT", "ATC"),
  stabilized = TRUE,
  truncate = c(0.01, 0.99)
)
}
\arguments{
\item{data}{A data.table containing propensity scores.}

\item{ps_col}{Character string specifying the propensity score column name. Default "ps".}

\item{treatment}{Character string specifying the treatment variable name.}

\item{weight_type}{Character string specifying weight type: "ATE", "ATT", or "ATC".}

\item{stabilized}{Logical; whether to use stabilized weights. Default TRUE.}

\item{truncate}{Numeric vector of length 2 specifying quantiles for weight truncation.
Default c(0.01, 0.99).}
}
\value{
A data.table with the original data plus:
\describe{
\item{weight}{IPTW weight}
}
}
\description{
Calculate inverse probability of treatment weights (IPTW) for causal inference.
}
\details{
Weight formulas:
\itemize{
\item ATE: \code{T/PS + (1-T)/(1-PS)}
\item ATT: \code{T + (1-T) * PS/(1-PS)}
\item ATC: \code{T * (1-PS)/PS + (1-T)}
}

Stabilized weights multiply by the marginal probability of treatment.
}
\examples{
\dontrun{
ps_data <- estimate_propensity_score(mydata, "treated", c("age", "sex"))
weighted_data <- calculate_weights(ps_data, treatment = "treated", weight_type = "ATE")
}

}
