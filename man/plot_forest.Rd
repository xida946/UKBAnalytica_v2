% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_forest}
\alias{plot_forest}
\title{Plot Forest Plot for Subgroup Analysis}
\usage{
plot_forest(
  results,
  estimate_col = "estimate",
  lower_col = "lower95",
  upper_col = "upper95",
  label_col = "subgroup",
  pvalue_col = "pvalue",
  p_interaction_col = "p_interaction",
  null_value = 1,
  log_scale = TRUE,
  colors = NULL,
  title = "Subgroup Analysis",
  xlab = "Hazard Ratio (95\% CI)",
  show_n = TRUE,
  show_events = TRUE
)
}
\arguments{
\item{results}{A data.frame from \code{run_subgroup_analysis()} or \code{run_multi_subgroup()}.}

\item{estimate_col}{Character string specifying the column name for effect estimates.
Default "estimate".}

\item{lower_col}{Character string specifying the column for lower CI. Default "lower95".}

\item{upper_col}{Character string specifying the column for upper CI. Default "upper95".}

\item{label_col}{Character string specifying the column for subgroup labels. Default "subgroup".}

\item{pvalue_col}{Character string specifying the column for p-values. Default "pvalue".}

\item{p_interaction_col}{Character string for interaction p-value column. Default "p_interaction".}

\item{null_value}{Numeric value for the null effect line. Default 1 (for HR/OR).}

\item{log_scale}{Logical; whether to use log scale for x-axis. Default TRUE.}

\item{colors}{Character vector of colors. Default NULL uses ggplot2 defaults.}

\item{title}{Character string for plot title. Default "Subgroup Analysis".}

\item{xlab}{Character string for x-axis label. Default "Hazard Ratio (95\\\% CI)".}

\item{show_n}{Logical; whether to show sample size. Default TRUE.}

\item{show_events}{Logical; whether to show event count. Default TRUE.}
}
\value{
A ggplot2 object.
}
\description{
Create a forest plot to visualize subgroup analysis results with effect estimates
and confidence intervals.
}
\examples{
\dontrun{
results <- run_multi_subgroup(data, exposure = "smoking",
                              subgroup_vars = c("sex", "age_group"),
                              model_type = "cox", endpoint = c("time", "status"))
plot_forest(results)
}

}
